<IfModule mod_rewrite.c>
    Options -MultiViews -Indexes
    RewriteEngine On

    # If the request is not already for /public, rewrite it to /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
